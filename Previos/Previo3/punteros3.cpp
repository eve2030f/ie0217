//Previo 3 B82870 Eveyn F.
#include <iostream> 
using namespace std; 

struct temp { }; // Define una estructura vacía llamada 'temp'

int i; // Declara una variable de tipo entero 
float f; // Declara una variable de tipo flotante 

int main() {
    temp *ptr; // Declara un puntero 'ptr' que apunta a objetos de tipo 'temp'

    return 0; 
}